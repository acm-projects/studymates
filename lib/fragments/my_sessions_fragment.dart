import 'package:flutter/material.dart';

class MySessionsFragment extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return new Center(
      child: new Text("My Sessions"),
    );
  }

}